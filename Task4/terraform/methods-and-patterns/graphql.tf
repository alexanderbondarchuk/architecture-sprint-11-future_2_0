# GraphQL API (Assess)

output "graphql_info" {
  value = "GraphQL API for flexible data queries under evaluation"
}

output "graphql_potential" {
  value = [
    "Flexible API for reports",
    "Reduced over-fetching",
    "Frontend data aggregation"
  ]
}